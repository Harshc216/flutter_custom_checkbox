import 'package:flutter/material.dart';

/// Default colors used throughout the package.
class CheckboxColors {
  CheckboxColors._();

  /// Checkbox fill color when selected.
  static const Color checkedColor = Colors.blue;

  /// Checkbox background when unselected.
  static const Color uncheckedColor = Colors.white;

  /// Border color.
  static const Color borderColor = Colors.grey;

  /// Check icon color.
  static const Color checkColor = Colors.white;

  /// Disabled checkbox color.
  static const Color disabledColor = Colors.grey;

  /// Disabled border color.
  static const Color disabledBorderColor = Color(0xFFBDBDBD);

  /// Error state color.
  static const Color errorColor = Colors.red;

  /// Hover color.
  static const Color hoverColor = Color(0xFFE3F2FD);

  /// Focus color.
  static const Color focusColor = Color(0xFF90CAF9);

  /// Splash color.
  static const Color splashColor = Color(0x332196F3);

  /// Shadow color.
  static const Color shadowColor = Color(0x33000000);

  /// Transparent color.
  static const Color transparent = Colors.transparent;
}